kind = "web"
previewPath = "/"
title = "BluePeak Digital Website"
version = "1.0.0"
id = "artifacts/bluepeak-site"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 22224

[services.development]
run = "pnpm --filter @workspace/bluepeak-site run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/bluepeak-site", "run", "build" ]
publicDir = "artifacts/bluepeak-site/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "22224"
BASE_PATH = "/"
